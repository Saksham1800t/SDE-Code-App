{
  "name": "sde-code",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SDE Code monorepo workspace root — an original, VS Code-inspired desktop IDE.",
  "scripts": {
    "lint": "eslint .",
    "lint:layers": "eslint . --rule-dir tools/lint-rules"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "@sde-code/eslint-plugin-layers": "workspace:*",
    "eslint": "^9.15.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild",
      "node-pty"
    ]
  }
}
