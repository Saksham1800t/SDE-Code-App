{
  "name": "@sde-code/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts for cross-process communication (main ↔ renderer, host ↔ extension-host). Single source of truth so both sides of an IPC/RPC boundary are checked by the compiler, not by matching string channel names by hand.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@sde-code/sdk": "workspace:*"
  }
}
